const host = "localhost"; // IP de la db
const user = "root"; // Nom d'utilisateur
const password = "mysql"; // Mot de passe
const database = "battleship"; // Base utilisée
const saltRounds = 10;

module.exports = {
    host,
    user,
    password,
    database,
    saltRounds,
}
