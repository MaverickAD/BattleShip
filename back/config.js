const host = "localhost"; // IP de la db
const user = "root"; // Nom d'utilisateur
const password = "root"; // Mot de passe
const database = "user"; // Base utilisée
const saltRounds = 10;

module.exports = {
    host,
    user,
    password,
    database,
    saltRounds,
}
